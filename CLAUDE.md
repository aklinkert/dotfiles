# CLAUDE.md — dotfiles

Personal dotfiles repo (github.com:aklinkert/dotfiles), default branch `master`.

## Git workflow (overrides global rule)

Push changes **directly to `master`** — no feature branch, no PR. This overrides
the global git-workflow rule (`~/.claude/rules/git-workflow.md`) per its own
project-CLAUDE.md exception. Still use conventional commit format.
